import { makeStyles } from "@material-ui/core";

export const useSideNavStyles = makeStyles({
    root: {
        padding: '10px',
    },
    item: {
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center'
    },
});