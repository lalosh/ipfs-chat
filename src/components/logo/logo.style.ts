import { makeStyles } from "@material-ui/core";

export const useLogoStyles = makeStyles({
    icon:{
        fontSize: '50px',
        color: 'white',
    },
    title:{
        color: 'white',
    }
});